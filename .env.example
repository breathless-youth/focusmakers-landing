# 이 파일을 .env.local 로 복사해 값을 채운다. 값이 없어도 랜딩은 동작한다.

# 베타 신청이 들어오면 알림을 보낼 Slack Incoming Webhook (선택).
# 비워 두면 알림 없이 접수만 한다.
SLACK_WEBHOOK_URL=

# TODO(저장소 연동): 신청을 DB 에 남기려면 여기에 자격증명을 추가하고
# app/api/beta/signup/route.ts 의 "신청 저장" 자리에 쓰기를 붙인다.
# 서버 전용 키는 NEXT_PUBLIC_ 접두사를 붙이지 않는다.
